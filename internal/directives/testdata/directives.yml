gateway:
  named: Gateway
  accessors: [path]

gateway.services:
  parent: Parent

gateway.services.*:
  parent: Parent
  accessors: [path]

gateway.services.*.endpoints:
  parent: Parent

gateway.services.*.endpoints.*:
  named: Endpoint
