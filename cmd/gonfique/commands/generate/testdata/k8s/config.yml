paths:
  <Config>.apiVersion: { declare: ApiVersion }
  <Config>.metadata.name: { declare: Name }
  <Config>.spec.**.containers: { declare: SpecContainers }
  <Config>.spec.ports.[]: { declare: Port }
  <Config>.spec.rules.[]: { declare: Rule }
  <Rule>.http.paths.[]: { declare: Path }
  <SpecContainer>.name: { declare: ContainerName }
  <SpecContainers>.[]: { declare: SpecContainer }
