meta:
  type: Config

rules:
  "**": { export: true }
  "**.tags.endpoints": { dict: map }
  "**.tags.endpoints.[value]": { declare: TagEndpoint }
  gateways.**.endpoints.*: { declare: Endpoint }

  <Endpoint>.method: { replace: http.Method test/http }
